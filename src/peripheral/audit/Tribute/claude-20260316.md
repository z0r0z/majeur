# [Claude (Opus 4.6)](https://claude.ai/) — Tribute.sol

**Prompt:** [`SECURITY.md`](../SECURITY.md) Tribute section (2-round methodology: defense verification + adversarial hunt)
**Type:** AI audit with structured 2-round methodology

## Review Summary

> **Reviewed 2026-03-16. No novel findings. 7/7 defenses verified, 6/6 invariants verified, 8 vectors examined and correctly discarded.**
>
> - **Quality is excellent.** The report follows the SECURITY.md methodology precisely: Round 1 produces a verdict for every defense and every invariant with line-number evidence. Round 2 examines 8 concrete attack vectors, screens each against the known findings and FP patterns, and correctly discards all of them. Zero false positives, zero duplicates.
> - **One miss: T-7** (stale ref resurrection on key reuse). The report examines "pagination stale-entry inflation" (vector 5) but classifies it as T-4/FP and discards. It doesn't consider the distinct semantic duplication issue: cancel/repost on the same `(proposer, dao, tribTkn)` key makes old refs resolve to the new active offer, producing duplicate entries in paginated views. ChatGPT 5.4 (Run 2) caught this; Opus 4.6 didn't. This is a Low view-only issue with no fund risk — a minor gap.
> - **Signal-to-noise: 0 novel from 0 reported (no false positives).** The correct outcome at this stage of audit convergence. The contract has been reviewed by 7 tools and the attack surface is stable.

---

## Round 1 — Defense Verification

All 7 defenses verified. All 6 invariants verified. Line references are slightly offset from the current file (likely from a paste), but all code paths traced are correct.

| Defense | Verdict |
|---------|---------|
| EIP-1153 reentrancy guard | Verified — all 3 mutating functions gated |
| CEI ordering | Verified — state writes before transfers in all paths |
| Bait-and-switch prevention | Verified — `TermsMismatch` check on all 3 terms |
| Overwrite guard | Verified — `tribAmt != 0` rejection |
| ETH/ERC20 mutual exclusion | Verified — branch conditions are exhaustive |
| Pagination bounds | Verified — `start >= len || limit == 0` early return |
| Solady safe transfers | Verified — extcodesize + returndatasize checks, FMP handling correct |

| Invariant | Verdict |
|-----------|---------|
| 1. ETH balance ≥ sum of active ETH tribAmt | Verified |
| 2. Active offers have tribAmt > 0, forAmt > 0 | Verified |
| 3. Mapping key uniqueness | Verified |
| 4. Proposer-only cancel, DAO-only claim | Verified |
| 5. claimTribute atomicity | Verified |
| 6. Ref arrays monotonically non-decreasing | Verified |

---

## Round 2 — Adversarial Hunt

8 vectors examined, all correctly discarded:

| # | Vector | Disposition |
|---|--------|-------------|
| 1 | ERC777/callback reentrancy | Blocked by nonReentrant + CEI |
| 2 | Same-token swap (tribTkn == forTkn) | State deleted before both transfers, no ordering issue |
| 3 | Self-tribute (dao == msg.sender) | Economically pointless, both legs settle correctly |
| 4 | Zero-address proposer | msg.sender cannot be address(0); dao == 0 rejected at line 78 |
| 5 | Pagination stale-entry inflation | Classified as T-4/FP — **missed T-7 semantic duplication** |
| 6 | Memory corruption across assembly blocks | Correctly identified as FP (Solady pattern) |
| 7 | ETH forwarding with insufficient gas | Self-inflicted (T-2) for adversarial proposers |
| 8 | msg.value reuse across branches | Exhaustive mutual exclusion verified |

---

## Evaluation Notes

**Missed T-7:** Vector 5 ("pagination stale-entry inflation") was correctly linked to T-4 and the "ref arrays can be spammed" FP pattern, but the report stopped there. The distinct issue is that cancel/repost on the same key doesn't just add stale entries — it makes old refs point to the *new* active offer, producing duplicate "active" results in view functions. This requires reasoning about what happens when a previously-used key becomes active again, not just about array growth. ChatGPT 5.4 (Run 2) caught this because its adversarial hunt focused on ref-to-mapping resolution semantics rather than just array size.

**Methodology compliance:** Excellent. The report follows the SECURITY.md instructions precisely — Round 1 produces verdicts for every defense and invariant, Round 2 screens against known findings and FP patterns before reporting. This is exactly the output the prompt was designed to produce.
