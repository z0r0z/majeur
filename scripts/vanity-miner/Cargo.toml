[package]
name = "vanity-miner"
version = "0.1.0"
edition = "2021"

[dependencies]
keccak-asm = "0.1"
digest = "0.10"
hex = "0.4"
rayon = "1.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"

[profile.release]
opt-level = 3
lto = true
